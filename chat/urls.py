from django.urls import path
from chat import views


urlpatterns = [
    # path('', views.index, name='index'),
    # path('<str:room_name>/', views.room, name='room'),
    # path('<str:chat_id>/', views.get_chat_for_user),
    # path('notification/', views.get_chat_for_user),
]